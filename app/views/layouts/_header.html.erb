<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "EventsBoard", root_path, class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="<%= 'active' if current_page?(root_path)%>"><%= link_to "Home", root_path %></li>
        <li class="<%= 'active' if current_page?(courses_path)%>"><%= link_to "Conversations", courses_path %></li>
        <li class="<%= 'active' if current_page?(events_path)%>"><%= link_to "Events", events_path %></li>

        <% admins_only do %>
          <li class="<%= 'active'if current_page?('/admin')%>"><%= link_to "Admin", admin_root_path %></li>
        <% end %>

        <% unless user_signed_in? %>
          <li class="<%= 'active' if current_page?(new_user_registration_path)%>"><%= link_to "Sign up", new_user_registration_path %></li>
          <li class="<%= 'active' if current_page?(new_user_session_path)%>"><%= link_to "Sign in", new_user_session_path %></li>
        <% end %>
      </ul>
<!--        <%= form_tag events_path, class: "navbar-form navbar-left", method: :get do  %>
          <div class="form-group">
            <%= text_field_tag :query, params[:query], placeholder: "Search events here..", class: "form-control" %>
          </div>
            <%= submit_tag "Submit", class: "btn btn-default" %>
        <% end %>
 -->
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
        <li class="<%= 'active' if current_page?(user_session_path)%>"><%= link_to edit_user_registration_path do%>
            <span></span> <%= current_user.username %>
          <% end %>
        <li>
        <li class="<%= 'active' if current_page?(user_session_path)%>"> <%= link_to "My Events", user_path(current_user) %>
        <li>
        <li><%= link_to "Sign out", destroy_user_session_path, method: :delete %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
